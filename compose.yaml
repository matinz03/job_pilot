services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - path: .env.local
        required: false
      - path: .env
        required: false
    ports:
      - "127.0.0.1:${HOST_PORT:-3102}:3000"
